# Unravel SQL Game Back
## Instalación
```bash
# Crear entorno virtual
python -m venv .venv

source .venv/bin/activate

# En Windows (PowerShell)
.venv\Scripts\activate

# Instalar dependencias
pip install -r requirements.txt

# Levantar la aplicación
flask run
